# === IDE / Editor ===
.idea/
.vscode/
*.iml
*.swp
*.swo
*~

# === OS ===
.DS_Store
Thumbs.db
desktop.ini

# === WorkBuddy internal ===
.workbuddy/

# === Java / Maven build output ===
target/
*.class
*.jar.original

# === 构建产物/备份 jar（仅保留源 jar） ===
*.jar.new
*.jar.bak-*

# === Maven Wrapper JAR (keep .cmd and properties, skip binary jar) ===
# Actually keep maven-wrapper.jar so mvnw works out of the box

# === H2 database runtime files ===
vote-backend-windows/data/
*.h2.db
*.lock.db
*.mv.db
*.trace.db

# === Logs ===
*.log
logs/

# === Uploaded files (runtime data) ===
uploads/
vote-backend-windows/uploads/
vote-backend/uploads/
